{% extends "::layout.html.twig" %}
{% block content %}

<legend>Machine</legend>
<form class="well machine">
    <div class="span12">
        <div class="row">
            <div class="span2">
                <label for="privateip">{{ 'machine.privateIp'|trans }}</label>
                <input id="privateip" type="text" class="input-medium petit" placeholder="{{ 'machine.privateIp'|trans }}">
            </div>
            <div class="span2">
                <label>{{ 'machine.publicIp'|trans }}</label>
                <input id="publicip" type="text" class="input-medium petit" placeholder="{{ 'machine.publicIp'|trans }}">
            </div>
            <div class="span3">
                <label>{{ 'machine.port'|trans }}</label>
                <input id="port" type="number" class="input-medium petit" placeholder="{{ 'machine.port'|trans }}">
            </div>
            <div class="span2">
                <label>{{ 'machine.user'|trans }}</label>
                <input id="user" type="text" class="input-medium petit" placeholder="{{ 'machine.user'|trans }}">  
            </div>
            <div class="span2">
                <label>{{ 'machine.passwd'|trans }}</label>
                <input id="user" type="text" class="input-medium petit" placeholder="{{ 'machine.passwd'|trans }}">  
            </div>               
        </div>
    </div>​
    <button id="publicip" type="submit" class="btn machine">Ajouter</button>
</form>
<table class="table table-striped">
    <thead>
        <tr>
            <th>{{ 'machine.id'|trans }}</th>
            <th>{{ 'machine.privateIp'|trans }}</th>
            <th>{{ 'machine.publicIp'|trans }}</th>
            <th>{{ 'machine.port'|trans }}</th>
            <th>{{ 'machine.user'|trans }}</th>
            <th>{{ 'machine.actions'|trans }}</th>
        </tr>
    </thead>
    <tbody>
            {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.privateIp }}</td>
                <td>{{ entity.publicIp }}</td>
                <td>{{ entity.port }}</td>
                <td>{{ entity.user }}</td>

                <td> 
                    <div class="btn-group">
                        <button class="btn"> <a href="{{ path('machine_show', { 'id': entity.id }) }}">{{ 'machine.show'|trans }}</a></button>
                        <button class="btn"><a href="{{ path('machine_edit', { 'id': entity.id }) }}">{{ 'machine.edit'|trans }}</a></button>
                        <button class="btn"> <a href="{{ path('machine_connectionTest', { 'id': entity.id }) }}">{{ 'machine.connectionTest'|trans }}</a></button>
                    </div>
                </td>
            </tr>  
            {% endfor %}
        </tbody>
    </table>

{% endblock %}