{% extends "::layout.html.twig" %}
{% block content %}
    <h1>{{ 'machine.editMachine'|trans }}</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('machine') }}">
                {{ 'machine.back'|trans }}
            </a>
        </li>
        <li>
            <form action="{{ path('machine_delete', { 'id': entity.id }) }}" method="post">
                {{ form_widget(delete_form) }}
                <button type="submit">{{ 'machine.del'|trans }}</button>
            </form>
        </li>
    </ul>

    <form action="{{ path('machine_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">{{ 'machine.edit'|trans }}</button>
        </p>
    </form>
{% endblock %}