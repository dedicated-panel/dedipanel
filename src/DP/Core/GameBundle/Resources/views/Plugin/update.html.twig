{#
    (c) Dedipanel <http://www.dedicated-panel.net>
    
    For the full copyright and license information, please view the LICENSE
    file that was distributed with this source code.
#}

{% extends "::layout.html.twig" %}

{% import 'SyliusResourceBundle:Macros:actions.html.twig' as sylius_actions %}
{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as sylius_buttons %}
{% import '::buttons.html.twig' as dedipanel_buttons %}

{% block title %}
    {{ dedipanel_buttons.back(path('dedipanel_plugin_show', {'id': plugin.id}), 'plugin.title.plugin'|trans) }}
{% endblock %}

{% block content %}
    <h2>{{ 'plugin.title.edit'|trans }}</h2>

    <form action="{{ path('dedipanel_plugin_update', { 'id': plugin.id }) }}" method="post" class="form-edit">    
        {{ form_widget(form) }}
        {{ sylius_actions.update(path('dedipanel_plugin_show', {'id': plugin.id})) }}    
    </form>
    
    {{ sylius_buttons.delete(path('dedipanel_plugin_delete', {'id': plugin.id})) }}
{% endblock %}
