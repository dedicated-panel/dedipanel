{% extends "::layout.html.twig" %}
{% block content %}
    <h1>{{ 'plugin.edit'|trans }}</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('game') }}">
                {{ 'plugin.back'|trans }}
            </a>
        </li>
        <li>
            <a href="{{ path('plugin') }}">
                {{ 'game.back'|trans }}
            </a>
        </li>
        <li>
            <form action="{{ path('plugin_delete', { 'id': entity.id }) }}" method="post">
                {{ form_widget(delete_form) }}
                <button type="submit">{{ 'game.del'|trans }}</button>
            </form>
        </li>
    </ul>

    <form action="{{ path('plugin_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">{{ 'game.edit'|trans }}</button>
        </p>
    </form>
{% endblock %}