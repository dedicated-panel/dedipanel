{% extends '::layout-online.html.twig' %}

{% block title %}
    {{ (baseRoute ~ '_admin.edit')|trans }}
{% endblock %}

{% block body -%}
    <h2>{{ 'admin.edit_title'|trans({ '%name': entity.name }) }}</h2>
    
    <ul id="top-nav" class="nav nav-tabs">
        <li>{{ form(delete_form) }}</li>
        <li>
            <a href="{{ path(baseRoute ~ '_admin') }}">
                {{ 'admin.back'|trans }}
            </a>
        </li>
    </ul>

    {{ form(edit_form) }}
{% endblock %}
