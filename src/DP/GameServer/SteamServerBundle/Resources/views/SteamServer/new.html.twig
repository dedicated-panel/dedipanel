{% extends "::layout.html.twig" %}
{% block content %}
    <h1>{{ 'steam.add'|trans }}</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('steam') }}">
                {{ 'steam.back'|trans }}
            </a>
        </li>
    </ul>

    <form action="{{ path('steam_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">{{ 'steam.addButton'|trans }}</button>
        </p>
    </form>
{% endblock %}