{% extends "::layout.html.twig" %}
{% block content %}
    <h1>{{ 'steam.editTitle'|trans }}</h1>

    <ul class="record_actions">
        <li>
            <a href="{{ path('steam') }}">
                {{ 'steam.back'|trans }}
            </a>
        </li>
        <li>
            <form action="{{ path('steam_delete', { 'id': entity.id }) }}" method="post">
                {{ form_widget(delete_form) }}
                <button type="submit">{{ 'steam.del'|trans }}</button>
            </form>
        </li>
    </ul>

    <form action="{{ path('steam_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">{{ 'steam.edit'|trans }}</button>
        </p>
    </form>
{% endblock %}